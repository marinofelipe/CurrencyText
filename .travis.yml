osx_image: xcode10.1
language: swift

cache:
- bundler
- cocoapods

install:
- bundle install
- pod repo update --silent
- pod install --project-directory=Example

script:
- pod lib lint

after_success: 
- cd Example && fastlane test